spring:
  application:
    name: TeamSync-Test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/teamsync?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: ${TEST_DB_PASSWORD:}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
