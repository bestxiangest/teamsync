# 站内通知数据库变更

## 执行时间

- 2026-06-07

## 数据库连接

- 主机：已脱敏，实际主机以本地运行配置或环境变量为准
- 端口：已脱敏，实际端口以本地运行配置或环境变量为准
- 数据库：postgres
- Schema：teamsync
- 账号：已脱敏，实际账号以本地运行配置或环境变量为准
- 密码：已脱敏，实际凭据以本地运行配置或环境变量为准
- JDBC：已脱敏，实际连接串以本地运行配置或环境变量为准

## 已执行脚本

- `doc/sql/sys_notification.sql`

## 新增对象

- 表：`teamsync.sys_notification`
- 唯一约束：`uq_sys_notification_dedupe (user_id, dedupe_key)`
- 外键：
  - `fk_sys_notification_user`
  - `fk_sys_notification_actor`
- 索引：
  - `idx_sys_notification_user_read_created`
  - `idx_sys_notification_user_type_created`
  - `idx_sys_notification_source`

## 去重口径

同一用户、同一 `dedupe_key` 只保留一条站内通知；重复扫描或重复事件命中唯一约束时不会新增通知。
